services:
  - type: web
    name: aadhaar-ocr-fallback
    env: python
    region: singapore
    plan: free
    buildCommand: "bash build.sh"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
